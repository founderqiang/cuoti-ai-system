# 安全政策

## 支持范围

项目目前处于早期迭代阶段，仅维护默认分支的最新版本。

## 报告安全问题

请通过 GitHub 个人主页提供的公开联系方式联系主维护者，并在标题中注明“cuoti-ai-system 安全问题”。在修复发布前，请不要公开漏洞利用代码、真实 API 密钥或学生数据。

报告中建议包含：

- 受影响的提交或版本
- 最小化复现步骤
- 影响说明
- 建议修复方式（如有）

## 安全边界

- 本项目默认用于教师本人控制的本地环境。
- 不应上传未经匿名化的学生个人信息。
- `ALLOW_UNSAFE_PLOT_CODE` 必须在公网部署中保持关闭。
- API 密钥应通过环境变量或本地 `.env` 管理，禁止提交到仓库。
